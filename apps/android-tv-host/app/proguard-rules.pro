# No rules needed yet: release builds run with isMinifyEnabled = false, and the
# app has no reflection-reached code of its own. The WebView @JavascriptInterface
# keep rule that used to live here went away with the WebView shell.
